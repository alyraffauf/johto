apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: seerr-johto
  namespace: arr
  annotations:
    tailscale.com/proxy-group: private-ingress
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - seerr
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: seerr
                port:
                  name: http
