apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: switchyard
  namespace: flux-system
spec:
  chart:
    spec:
      chart: ./k8s/charts/switchyard
      sourceRef:
        kind: GitRepository
        name: flux-system
      version: 0.1.0
  install:
    createNamespace: true
    remediation:
      retries: 3
  interval: 30m
  releaseName: switchyard
  targetNamespace: switchyard
  timeout: 10m
